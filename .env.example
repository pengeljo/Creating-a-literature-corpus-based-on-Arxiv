# Environment variables for arxiv-corpus
# Copy this file to .env and customize

# =============================================================================
# Database Connections
# =============================================================================

# MongoDB connection
MONGODB_URI=mongodb://localhost:27017

# Qdrant vector database (for semantic search)
QDRANT_URL=http://localhost:6333

# =============================================================================
# Docker Volume Paths (optional - defaults to Docker named volumes)
# =============================================================================
# Set these to use bind mounts instead of Docker volumes.
# Useful for placing data on specific disks or for easier backups.

# MongoDB data directory (uncomment to use bind mount)
# MONGODB_DATA_PATH=./data/mongodb

# Qdrant data directory (uncomment to use bind mount)
# QDRANT_DATA_PATH=./data/qdrant

# =============================================================================
# Docker Port Configuration (optional)
# =============================================================================
# Change these if you have port conflicts

# MONGODB_PORT=27017
# QDRANT_REST_PORT=6333
# QDRANT_GRPC_PORT=6334

# =============================================================================
# Application Settings
# =============================================================================

ARXIV_CORPUS_CONFIG=config/default.yaml
ARXIV_CORPUS_LOG_LEVEL=INFO

# Optional: spaCy model (override config)
# SPACY_MODEL=en_core_web_lg

# Optional: OpenAI API key (for OpenAI embeddings instead of sentence-transformers)
# OPENAI_API_KEY=sk-...
